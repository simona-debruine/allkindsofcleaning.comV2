# Property Enrichment Engine (server-side only — Vite BFF / Amplify enrich BFF)
PROPERTY_ENRICHMENT_URL=https://9ug42crh6h.execute-api.us-east-1.amazonaws.com/prod
PROPERTY_ENRICHMENT_API_KEY=

# Amplify production: Function URL from `infra/enrich-bff/deploy.sh`
# (keeps the PEE API key off the client; required for estimate address lookup on Amplify)
VITE_ENRICH_BFF_URL=

# Cleaning Intelligence Engine HTTP API (Architecture v1.0).
# Leave empty to use the local contract adapter until CIE AWS HTTP is live.
VITE_CIE_API_URL=
