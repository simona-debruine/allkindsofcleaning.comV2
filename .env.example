# Property Enrichment Engine (server-side only — used by Vite BFF / Amplify Function)
PROPERTY_ENRICHMENT_URL=https://9ug42crh6h.execute-api.us-east-1.amazonaws.com/prod
PROPERTY_ENRICHMENT_API_KEY=

# Cleaning Intelligence Engine HTTP API (Architecture v1.0).
# Leave empty to use the local contract adapter (browser-side math + PEE proxy).
# When CIE is deployed on AWS, set to the API Gateway base URL, e.g.:
#   https://xxxx.execute-api.us-east-1.amazonaws.com/prod
# Endpoints: /property/enrich, /expected-cleaning-profile, /estimate-labor, /estimate-price
VITE_CIE_API_URL=
